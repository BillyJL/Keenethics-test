import classes from "./index.module.css";

const Header = () => {
    return (
        <header className={classes.header}>
            <span className={classes.title}>ADMIN.BIKE-BOOKING.COM</span>
        </header>
    );
};

export default Header;
